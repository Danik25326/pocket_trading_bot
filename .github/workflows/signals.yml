name: Generate Trading Signals

on:
  workflow_dispatch:
    inputs:
      language:
        description: 'Мова для генерації сигналів'
        required: true
        default: 'uk'
        type: choice
        options:
          - uk
          - ru
      trigger_source:
        description: 'Джерело запуску'
        default: 'manual'
        type: string
  schedule:
    - cron: '*/10 * * * *'  # Кожні 10 хвилин

permissions:
  contents: write

jobs:
  generate-signals:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Clear all caches
      run: |
        echo "🧹 Очищення кешу..."
        pip cache purge
        rm -rf ~/.cache/pip
        rm -rf ~/.cache/apt
        sudo apt-get clean
        echo "✅ Кеш очищено"
    
    - name: Upgrade pip and install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --upgrade setuptools wheel
        pip install --no-cache-dir -r backend/requirements.txt
    
    - name: Create necessary directories
      run: |
        mkdir -p data
        mkdir -p logs
        touch .nojekyll
        
        # Створюємо початкові файли, якщо їх немає
        if [ ! -f data/signals.json ]; then 
          echo '{"last_update": null, "signals": [], "timezone": "Europe/Kiev (UTC+2)"}' > data/signals.json
        fi
        if [ ! -f data/history.json ]; then 
          echo '[]' > data/history.json
        fi
        if [ ! -f data/assets_config.json ]; then 
          echo '{"preferred_assets": ["GBPJPY_otc", "EURUSD_otc", "USDJPY_otc"]}' > data/assets_config.json
        fi
        if [ ! -f data/lessons.json ]; then 
          echo '[]' > data/lessons.json
        fi
    
    - name: Generate trading signals
      env:
        POCKET_SSID: ${{ secrets.POCKET_SSID }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        POCKET_DEMO: 'true'
        SIGNAL_INTERVAL: '600'  # 10 хвилин
        MIN_CONFIDENCE: '0.7'
        MAX_DURATION: '5.0'
        SIGNAL_VALIDITY_MINUTES: '10'  # Сигнали зникають через 10 хвилин
        MAX_SIGNALS_ON_SITE: '6'       # Макс 6 сигналів на сайті
        ASSETS: 'GBPJPY_otc,EURUSD_otc,USDJPY_otc'
        TIMEFRAMES: '120'
        LOG_LEVEL: 'INFO'
        GROQ_MODEL: 'openai/gpt-oss-120b'
        LANGUAGE: ${{ inputs.language || 'uk' }}
        TRIGGER_SOURCE: ${{ inputs.trigger_source || 'manual' }}
      run: |
        echo "🚀 Початок генерації сигналів..."
        echo "🌐 Мова: $LANGUAGE"
        echo "📊 Активи: $ASSETS"
        echo "🧠 Модель AI: $GROQ_MODEL"
        echo "🕐 Часовий пояс: Київ (UTC+2)"
        echo "🔧 Джерело запуску: $TRIGGER_SOURCE"
        echo "⏱️  Інтервал генерації: 10 хвилин"
        echo "📈 Сигналів на сайті: максимум 6"
        
        export PYTHONPATH=$PYTHONPATH:$(pwd)/backend:$(pwd):$(pwd)/utils
        
        cd backend
        python signal_generator.py
        
        echo "✅ Генерація сигналів завершена"
    
    - name: Generate config.js
      run: |
        echo "// config.js - automatically generated" > config.js
        echo "window.GH_CONFIG = {" >> config.js
        echo "    token: '${{ secrets.GH_PAT }}'," >> config.js
        echo "    owner: 'Danik25326'," >> config.js
        echo "    repo: 'pocket_trading_bot'," >> config.js
        echo "    workflowId: 'signals.yml'" >> config.js
        echo "};" >> config.js
        
        echo "✅ Config.js generated"
        cat config.js
    
    - name: Commit and push all changes
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'
        
        git add data/
        git add logs/
        git add .nojekyll
        git add config.js
        
        if git diff --staged --quiet; then
          echo "⚠️ Немає змін для коміту"
        else
          git commit -m "🤖 Сигнали: $(date +'%Y-%m-%d %H:%M') [Мова: ${{ inputs.language || 'uk' }}] [skip ci]"
          git push origin main
          echo "✅ Зміни закомічено та відправлено"
        fi
